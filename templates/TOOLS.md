# TOOLS.md

Tool-specific guidance for this workspace. Conventions, gotchas, when to reach for what.

## Preferred tools

- (e.g. `rg` over `grep`; `fd` over `find`)
- (e.g. specific MCP servers, CLIs)

## Tools-first rule

Before writing a script:

1. Check if a tool already covers this.
2. If yes, use it. Update notes here.
3. If no, then code. Document why no tool fits.

## Gotchas

- (e.g. "tool X requires --no-pager when piped")
